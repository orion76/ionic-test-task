import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'test-task-store',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
